# Git Workflow Skill

Git 버전 관리 및 협업 워크플로우를 위한 스킬입니다.

## 핵심 역할

1. **브랜치 전략**: 일관된 브랜치 관리
2. **커밋 메시지 규칙**: 표준화된 커밋 메시지
3. **PR 관리**: 효과적인 Pull Request 프로세스
4. **릴리스 관리**: 버전 관리 및 배포

## 브랜치 전략

### 📊 브랜치 구조
```
main (production)
├── develop (staging)
│   ├── feature/[feature-name]
│   ├── fix/[bug-name]
│   └── refactor/[refactor-name]
├── hotfix/[hotfix-name]
└── release/[version]
```

### 🏷️ 브랜치 명명 규칙

| 타입 | 형식 | 예시 |
|------|------|------|
| Feature | `feature/[issue-id]-description` | `feature/123-add-login` |
| Bug Fix | `fix/[issue-id]-description` | `fix/456-fix-auth-error` |
| Hotfix | `hotfix/[version]-description` | `hotfix/1.2.1-security-patch` |
| Refactor | `refactor/[scope]-description` | `refactor/auth-cleanup` |
| Release | `release/[version]` | `release/1.3.0` |

### 🔄 브랜치 생명주기

```
1. feature/* → develop (PR merge)
2. develop → release/* (릴리스 준비)
3. release/* → main (프로덕션 배포)
4. main → develop (역머지)
5. hotfix/* → main & develop (긴급 수정)
```

## 커밋 메시지 규칙

### 📝 기본 형식 (한국어)
```
<타입>: <제목>

[본문 (선택)]

[꼬리말 (선택)]
```

### 🏷️ 커밋 타입

| 타입 | 설명 | 예시 |
|------|------|------|
| `feat` | 새로운 기능 | feat: 로그인 기능 추가 |
| `fix` | 버그 수정 | fix: 인증 토큰 만료 오류 수정 |
| `docs` | 문서 변경 | docs: README 업데이트 |
| `style` | 코드 스타일 (포맷팅) | style: 코드 포맷팅 적용 |
| `refactor` | 리팩토링 | refactor: 인증 모듈 구조 개선 |
| `test` | 테스트 추가/수정 | test: 로그인 단위 테스트 추가 |
| `chore` | 빌드/설정 변경 | chore: 의존성 업데이트 |
| `perf` | 성능 개선 | perf: 쿼리 최적화 |

### 📋 좋은 커밋 메시지 예시

```
feat: 사용자 프로필 이미지 업로드 기능 추가

- 이미지 리사이징 및 최적화 구현
- S3 업로드 연동
- 프로필 페이지 UI 업데이트

Closes #123
```

```
fix: 결제 실패 시 재시도 로직 오류 수정

기존: 재시도 횟수가 초기화되지 않음
수정: 성공 시 재시도 카운터 리셋

Fixes #456
```

### ❌ 나쁜 커밋 메시지

```
❌ fix: 버그 수정
❌ update
❌ WIP
❌ 수정함
❌ asdfasdf
```

## Pull Request 프로세스

### 📋 PR 템플릿
```markdown
## 📝 변경 사항
[변경 내용 요약]

## 🔗 관련 이슈
- Closes #[issue-number]

## 🧪 테스트
- [ ] 단위 테스트 추가/수정
- [ ] 통합 테스트 통과
- [ ] 수동 테스트 완료

## 📸 스크린샷 (UI 변경 시)
[스크린샷 첨부]

## ✅ 체크리스트
- [ ] 코드 셀프 리뷰 완료
- [ ] 문서 업데이트 (필요시)
- [ ] 변경 사항 테스트 완료
```

### 🔍 PR 리뷰 체크리스트
- [ ] CI 빌드 통과
- [ ] 테스트 커버리지 유지/증가
- [ ] 코드 품질 기준 충족
- [ ] 보안 검토 완료 (민감한 변경 시)

## 릴리스 관리

### 🏷️ 버전 규칙 (Semantic Versioning)
```
MAJOR.MINOR.PATCH

MAJOR - 호환성 깨지는 변경
MINOR - 새 기능 (하위 호환)
PATCH - 버그 수정
```

### 📦 릴리스 프로세스

```bash
# 1. 릴리스 브랜치 생성
git checkout develop
git checkout -b release/1.3.0

# 2. 버전 업데이트
npm version 1.3.0 --no-git-tag-version

# 3. 변경사항 검증
npm run test
npm run build

# 4. PR 생성 (release → main)

# 5. 머지 후 태그 생성
git checkout main
git pull
git tag -a v1.3.0 -m "Release v1.3.0"
git push origin v1.3.0

# 6. develop 역머지
git checkout develop
git merge main
git push origin develop
```

## Git 명령어 참조

### 📋 일상 워크플로우
```bash
# 브랜치 생성 및 이동
git checkout -b feature/new-feature

# 변경사항 스테이징
git add .

# 커밋
git commit -m "feat: 새로운 기능 추가"

# 푸시
git push origin feature/new-feature

# 최신 develop 가져오기
git fetch origin
git rebase origin/develop
```

### 🔄 유용한 명령어
```bash
# 마지막 커밋 수정
git commit --amend

# 인터랙티브 리베이스
git rebase -i HEAD~3

# 변경사항 임시 저장
git stash
git stash pop

# 특정 커밋 체리픽
git cherry-pick <commit-hash>

# 변경 이력 확인
git log --oneline -10
```

## 충돌 해결

### 🔧 기본 프로세스
```bash
# 1. 충돌 발생 시 상태 확인
git status

# 2. 충돌 파일 수정
# <<<<<<< HEAD
# 현재 브랜치 내용
# =======
# 머지되는 브랜치 내용
# >>>>>>> feature/other

# 3. 충돌 해결 후 스테이징
git add <resolved-files>

# 4. 머지 계속
git merge --continue
# 또는
git rebase --continue
```

### ⚠️ 주의사항
- 항상 충돌 파일을 수동으로 검토
- 테스트 실행하여 해결 검증
- 불확실하면 팀원과 상의
